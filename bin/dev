#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
host="${OLIVE_WPT_HOST:-0.0.0.0}"
port="${OLIVE_WPT_PORT:-8000}"

cd "$repo_root"
exec uv run uvicorn app:app --app-dir "$repo_root" --host "$host" --port "$port" --reload "$@"
